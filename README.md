# tiny-graph-optimizer

## graph

graph is combining of matrix expression and matrix operations, e.g. plus, multiply, leru, digonalizing

## simple ir

simple ir is the code level expression for graph operations
